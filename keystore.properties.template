# TASK 2. Copy this file to "keystore.properties" (same folder, project
# root -- NOT inside app/) and fill in real values. Add "keystore.properties"
# to .gitignore; never commit the filled-in version or the .jks file itself.
#
# storeFile is relative to the project root.

storeFile=../interval-timer-release.jks
storePassword=CHANGE_ME
keyAlias=interval_timer_key
keyPassword=CHANGE_ME
